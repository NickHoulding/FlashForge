[project]
name = "flashforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.1.1",
]

[tool.uv]
package = true

[project.scripts]
flashforge = "flashforge.__main__:main"
